apiVersion: 1

providers:
  - name: EduAssist Dashboards
    orgId: 1
    folder: EduAssist
    type: file
    disableDeletion: false
    updateIntervalSeconds: 15
    allowUiUpdates: false
    options:
      path: /etc/grafana/dashboards
